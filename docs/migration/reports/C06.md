# C06 handoff

- Task: C06 Native workflow controls and repository scope
- Status: BLOCKED
- development_status: READY
- acceptance_status: BLOCKED
- Starting commit(s):
  - gsd-pi `migration/raidnight-native` `6ed641dce9ac6797cd28ef5cbe08b1ad9010ba9f` (C05 commit; this task's implementation is uncommitted)
  - C00 pin ancestor remains `fa83b795f3ef5fd7d2c02b37d4e96cbd9dc94f88`
  - raid-night `migration/gsd-native` was not modified
  - pack/schema unchanged: `contracts/interface.schema.json` SHA256 `0643f017003901c73859db55ae74dbc0bb67b270e43554a7111ca8038d8aca01`
- Resulting commit(s), or explicit uncommitted-file list: local C06 task commit on `migration/raidnight-native` with the files listed below.
- Contract/pack version and schema SHA256: pack 1.0.0; schema SHA256 `0643f017003901c73859db55ae74dbc0bb67b270e43554a7111ca8038d8aca01` (schema not altered)
- Changed files:
  - `src/runtime-control/native-commands.ts` (typed prepare/review/replan/start/resume/cancel/recover adapters; test injects only)
  - `src/runtime-control/prepare-boundary.ts` (research/plan only; halt before execute-task)
  - `src/runtime-control/command-handlers.ts` (production default is native adapters; `registerCommandHandlerForTest` kept)
  - `src/runtime-control/admission.ts` (always invoke handler; persist handler result before dispatch-intent)
  - `src/runtime-control/import-jobs.ts` (POST JobImport, digest fingerprint, durable catalog, 409 overwrite)
  - `src/runtime-control/job-catalog.ts` (durable jobs.json)
  - `src/runtime-control/fingerprint.ts` (`fingerprintImport` / `fingerprintAnswer`)
  - `src/runtime-control/answers.ts` (pending-question routing, no second lease)
  - `src/runtime-control/cancel.ts` / `idle-probe.ts` / `recovery.ts` (R8 cancel, fail-closed idle probe, explicit recovery_id)
  - `src/runtime-control/workspace-profile.ts` (S6 one-writable-target, bwrap argv, fail-closed preflight)
  - `src/runtime-control/registration.ts` (within-project alias/nesting rejected; realpath cache roots; profile install)
  - `src/runtime-control/capabilities.ts` (`milestone_scope` and `readonly_references` true)
  - `src/runtime-control/control.ts` (issue recovery_id on crash reconcile)
  - `src/runtime-control/errors.ts` / `types.ts` / `index.ts`
  - `src/runtime-control/tests/c06-native.test.ts` (AT-C05, AT-C06, AT-C07, AT-C08, AT-W01, AT-W02)
  - `src/runtime-control/tests/admission.test.ts` / `entry-guard.test.ts` / `http-routes.test.ts` / `harness.ts`
  - `src/resources/extensions/gsd/auto/dispatch.ts` (prepare boundary on resolveDispatch)
  - `src/resources/extensions/gsd/dispatch-guard.ts` (scoped auto refuses other milestones)
  - `src/resources/extensions/gsd/host-check-runner.ts` / `exec-sandbox.ts` / `packages/pi-coding-agent/src/core/tools/bash.ts` (bwrap wrap, fail closed)
  - `src/resources/extensions/gsd/bootstrap/write-gate.ts` / `packages/pi-coding-agent/src/core/tools/write.ts` / `edit.ts` (canonical write guard)
  - `src/resources/extensions/gsd/native-git-bridge.ts` (trusted Git host path; neighbours refused)
  - `src/resources/extensions/gsd/post-execution-checks.ts` / `verification-evidence.ts` (out-of-contract target changes rejected without delete)
  - `src/resources/extensions/gsd/replan-evidence.ts` plus `tools/replan-task.ts` / `tools/replan-slice.ts`
  - `web/app/api/runtime/v1/projects/[project_id]/jobs/route.ts` (POST import)
  - `web/app/api/runtime/v1/jobs/[job_id]/answers/route.ts` (POST answers)
  - `web/app/api/runtime/v1/_shared.ts`
  - `docs/migration/reports/C06.md`
  - `PROGRESS.md`
- Exact commands run and exit results:
  - gsd-pi has no `.venv`; Node path used. pnpm invoked as `npx --yes pnpm@10.12.1`.
  - `npx --yes pnpm@10.12.1 run typecheck:extensions` exit 0
  - `node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test --test-concurrency=1 src/runtime-control/tests/admission.test.ts src/runtime-control/tests/entry-guard.test.ts src/runtime-control/tests/http-routes.test.ts src/runtime-control/tests/c06-native.test.ts` 38 passed, exit 0
  - `node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test --test-concurrency=1 src/resources/extensions/gsd/tests/dispatch-guard.test.ts src/resources/extensions/gsd/tests/required-policy.test.ts` 34 passed, exit 0
  - `node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test --test-concurrency=1 src/resources/extensions/gsd/tests/exec-sandbox.test.ts src/resources/extensions/gsd/tests/post-execution-checks.test.ts` 84 passed, exit 0
- Acceptance cases:
  - AT-C05 prepare performs no implementation: expected prepare-mode dispatch to halt at `execute-task`, `validatePlan` required, product files unchanged, `succeeded` only at prepared boundary. Observed PASS. Evidence: `src/runtime-control/tests/c06-native.test.ts`.
  - AT-C06 scoped auto does not spill: expected start/resume to set `milestoneLock` / `GSD_MILESTONE_LOCK`; dispatch-guard rejects other milestone units. Observed PASS. Evidence: `c06-native.test.ts`, `dispatch-guard.test.ts`.
  - AT-C07 cancel really frees ownership: expected immediate `cancelling`, stop/abort plus injected idle probe to release lease; missing probe retains lease as `recovery_required`; late PASS refused. Observed PASS. Evidence: `c06-native.test.ts`.
  - AT-C08 answers: expected scope plus request-id validation, stale epoch 409, duplicate different value 409, duplicate same value returns the receipt, no extra lease. Observed PASS. Evidence: `c06-native.test.ts`, `http-routes.test.ts`.
  - AT-W01 target/reference boundaries: expected native write to neighbour to fail, neighbour read to work, target/scratch/trusted Git host ops to work, shell/Python without bwrap blocked with diagnostics. Observed PASS on this Mac (fail-closed). Linux kernel RO-bind of neighbours: NOT_RUN. Evidence: `c06-native.test.ts`.
  - AT-W02 traversal/symlink: expected `../`, symlink alias, and absolute neighbour paths rejected after realpath; neighbour bytes unchanged. Observed PASS for canonical path policy. Linux bwrap EROFS mount: NOT_RUN. Evidence: `c06-native.test.ts`.
  - C05 AT-C01-C04 remain green. GET snapshots/events/history still 503. Observed PASS. Evidence: `admission.test.ts`, `entry-guard.test.ts`, `http-routes.test.ts`.
- Baseline failures still present:
  - C00 overall BLOCKED: Linux bwrap, systemd/cgroup, model idle probe, legacy spec roots, physical Quake; Intel Mac RDKit wheel gap.
  - C02 overall BLOCKED, `development_status: READY`: AT-S01 RDKit derivation still needs the lockfile wheel on Linux or Apple Silicon. Not waived.
- Checks NOT_RUN and why:
  - Linux kernel read-only mount enforcement for AT-W01/W02 (shell/Python write via bwrap actually EROFS) is NOT_RUN on this Darwin host. Owner: C06 Linux follow-up. Fail-closed diagnostics on Mac are not treated as that kernel evidence.
  - Live llama.cpp / production services / unrestricted shell to "prove" neighbour writes: not run, by design.
  - Full `pnpm test` not run; targeted typecheck plus C05 admission/HTTP/entry-guard, C06 native/workspace, dispatch-guard, required-policy, exec-sandbox, and post-execution regressions were the C06 evidence set.
  - No live GSD, monitor, or inference endpoints contacted. No production services started. No managed model job dispatched. No Mac sandbox-exec or launchd substitute.
- New runtime/science assumptions:
  - Runtime-v1 JobImport is not GSD markdown Import Application (`applyLegacyImport` requires preview/backup/consent). Import identity is `insertMilestone` plus a digest-keyed identity file under `.gsd/imports/<digest>/` and the durable job catalog. Same digest plus same payload fingerprint is idempotent; changed payload targeting an existing digest is 409 and requires explicit replan.
  - Production start/resume set `GSD_MILESTONE_LOCK` and workspace implement phase; they do not dispatch a live auto loop from HTTP admission. Tests inject `registerNativeWorkflowOpsForTest`.
  - Idle probe is fail-closed when `backend_idle_probe` is null. Tests inject only through `registerIdleProbeForTest`.
  - S6 bubblewrap argv is built for registered workspace profiles; Darwin preflight fails closed. No `--no-sandbox` and no sandbox-exec.
- Secrets/production data touched: none
- Next permitted task: C07 (snapshots, SSE, history). Do not implement C08 FTM chemistry or dispatch live managed model jobs in C07. C07 may proceed under the Mac addendum exception because the outstanding C06 gap is an allowed Linux bubblewrap/kernel-mount category.
- Remaining blocker, owner task and minimal correction:
  - C06 overall BLOCKED pending Linux AT-W01/W02 kernel RO-bind evidence. Owner: C06 Linux follow-up on a host with bwrap user namespaces. Portable `development_status` is READY.
  - C00 overall remains BLOCKED pending Linux rerun; C02 overall remains BLOCKED pending RDKit. Those do not block portable C07 under the addendum exception.

Native prepare/review/replan/start/resume/cancel/recover reuse existing GSD dispatch policy, `milestoneLock`, stop/abort, C03 process-group cleanup, and verification evidence invalidation. One writable target; neighbours are read-only. Import and answers are durable receipts without a model lease.

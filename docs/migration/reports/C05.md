# C05 handoff

- Task: C05 Implement typed runtime admission and durable operation receipts
- Status: PASS
- development_status: READY
- acceptance_status: PASS
- Starting commit(s):
  - gsd-pi `migration/raidnight-native` `7dacd57a2f641c9571067c9b321e0ef911abcba5` (C04 commit; this task's implementation is uncommitted)
  - C00 pin ancestor remains `fa83b795f3ef5fd7d2c02b37d4e96cbd9dc94f88`
  - raid-night `migration/gsd-native` `d31b87399a491bd4b7ac0df65189c5b3c9ebd6fe` (C02 portable READY / overall BLOCKED; not modified)
  - pack/schema unchanged: `contracts/interface.schema.json` SHA256 `0643f017003901c73859db55ae74dbc0bb67b270e43554a7111ca8038d8aca01`
- Resulting commit(s), or explicit uncommitted-file list: local C05 task commit on `migration/raidnight-native` with the files listed below.
- Contract/pack version and schema SHA256: pack 1.0.0; schema SHA256 `0643f017003901c73859db55ae74dbc0bb67b270e43554a7111ca8038d8aca01` (schema copied verbatim; not altered)
- Changed files:
  - `src/runtime-control/` (new: registration, capabilities, fingerprint, job catalog, atomic JSON receipts, operation store+index, model lease, command handlers, R3-R5 admission, entry guard, daemon singleton)
  - `src/runtime-control/tests/admission.test.ts` (AT-C01, AT-C02, AT-C03)
  - `src/runtime-control/tests/entry-guard.test.ts` (AT-C04)
  - `src/runtime-control/tests/http-routes.test.ts`
  - `src/runtime-control/tests/harness.ts`
  - `web/app/api/runtime/v1/` (capabilities, projects, commands, operation lookup; C06/C07 routes return 503 `runtime_unavailable`)
  - `web/app/api/session/command/route.ts` (registered FTM model RPC guarded)
  - `web/app/api/terminal/sessions/route.ts` (managed `gsd` terminal start guarded)
  - `web/app/api/terminal/stream/route.ts` (same guard before create-on-connect)
  - `packages/contracts/runtime-v1/` (verbatim C01 copy plus `PRODUCER.md`)
  - `packages/contracts/src/runtime-v1-manifest.test.ts`
  - `packages/contracts/package.json` (hash-verify test wired)
  - `tsconfig.json` (exclude `src/runtime-control` from CLI `tsc`, matching `src/web`)
  - `tsconfig.extensions.json` (typecheck `src/runtime-control`, exclude its tests)
  - `docs/migration/reports/C05.md`
  - `PROGRESS.md`
- Exact commands run and exit results:
  - gsd-pi has no `.venv`; Node path used. pnpm invoked as `npx --yes pnpm@10.12.1`.
  - `npx --yes pnpm@10.12.1 run typecheck:extensions` exit 0
  - `npx --yes pnpm@10.12.1 --filter @opengsd/contracts run build` exit 0
  - `npx --yes pnpm@10.12.1 --filter @opengsd/contracts test` 8 passed, exit 0 (includes C01 manifest SHA256 verify)
  - `node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test src/runtime-control/tests/admission.test.ts src/runtime-control/tests/entry-guard.test.ts src/runtime-control/tests/http-routes.test.ts src/resources/extensions/gsd/tests/required-policy.test.ts` 32 passed, exit 0
- Acceptance cases:
  - AT-C01 admission: expected parallel starts across two registered projects to produce one 202 admission and one 409 `model_busy`. Observed PASS. Evidence: `src/runtime-control/tests/admission.test.ts`.
  - AT-C02 idempotency: expected same request_id + fingerprint to return the same receipt after store reopen; different payload 409 `request_id_conflict`; fingerprint checked before stale revision; stale epoch with larger revision 409 `revision_conflict`. Observed PASS. Evidence: `admission.test.ts`.
  - AT-C03 crash window: expected crash before receipt to leave no operation so a retry can admit once; crash after receipt before index to rebuild lookup without dispatch; crash after dispatch intent to `recovery_required` without replaying the handler. Observed PASS. Evidence: `admission.test.ts`.
  - AT-C04 all-entry-point guard: expected missing `ftm-science/v1` bind and failed self-check to block generic `POST /api/session/command` prompts and managed `gsd` terminal starts with 503 `policy_unavailable`; read-only snapshots and unregistered cwd remain unblocked. Observed PASS. Evidence: `src/runtime-control/tests/entry-guard.test.ts`.
  - Supporting: production handlers without test inject return 202 accepted then persist `failed`/`runtime_unavailable` (never `succeeded`); C06/C07 routes 503; GET capabilities reports honest feature flags and no model calls. Observed PASS. Evidence: `admission.test.ts`, `http-routes.test.ts`, `entry-guard.test.ts`.
- Baseline failures still present:
  - C00 overall BLOCKED: Linux bwrap, systemd/cgroup, model idle probe, legacy spec roots, physical Quake; Intel Mac RDKit wheel gap.
  - C02 overall BLOCKED, `development_status: READY`: AT-S01 RDKit derivation still needs the lockfile wheel on Linux or Apple Silicon. Not waived.
  - C00 recorded: RaidNight `tests/test_monitor_service.py::test_monitor_service_path_includes_user_local_bin`; `tests/test_preferred_pipeline_job.py::test_jobs_pipeline_snapshot_includes_stable_display_fields`; `tests/test_shell_completion.py` two workspace-resolution failures; Quake frontend `npm ci` lockfile mismatch; GSD postinstall runtime-dependency materialize warning.
- Checks NOT_RUN and why:
  - Linux C00 items remain NOT_RUN (bwrap, systemd/cgroup, model idle probe, legacy spec roots, physical Quake). C05 is portable Node admission work and does not claim those.
  - C02 AT-S01 RDKit derivation remains NOT_RUN on this Intel Mac. Owner: C02 on a host with the locked RDKit wheel. C05 does not treat that miss as waived Linux science evidence.
  - Full `pnpm test` not run; targeted typecheck plus contracts hash-verify, C05 admission/HTTP/entry-guard, and C03 required-policy regression were the C05 evidence set.
  - No live GSD, monitor, or inference endpoints contacted. No production services started. No managed model job dispatched.
- New runtime/science assumptions: none. Runtime-v1 is admission/receipt machinery on the existing web/RPC daemon, not a second scheduler. Registration is a read-only JSON file (`GSD_RUNTIME_REGISTRATION`); clients submit project IDs. Durable receipts are atomic per-operation JSON files plus a request-id index under the GSD state root, single in-process writer. Uncertain dispatch reconciles to `recovery_required` and is not auto-replayed. Exactly-once arbitrary tools are not promised. Feature flags `milestone_scope`, `readonly_references`, `project_snapshots`, and `event_history` remain false until C06/C07. Production command actions report not-ready until C06; test handlers are injected only through `registerCommandHandlerForTest`.
- Secrets/production data touched: none
- Next permitted task: C06 (native commands and workspace). Do not start it in this task. Do not implement C07 snapshots/SSE or C08 FTM chemistry.
- Remaining blocker, owner task and minimal correction:
  - None for C05 criteria.
  - C00 overall remains BLOCKED pending Linux rerun; C02 overall remains BLOCKED pending RDKit. Those do not block portable C06 under the Mac addendum exception.

Runtime-v1 command admission reuses existing bearer auth, BridgeService workers, and the C03 required-policy registry. Parallel starts across projects share one model lease. Duplicate request IDs survive restart. Ambiguous dispatch is `recovery_required`. Missing required policy cannot start through a generic web prompt or a managed `gsd` terminal.
